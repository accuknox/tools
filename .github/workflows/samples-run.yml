name: Run samples

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  go-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v2

      - name: Build accuknox CLI 
        run: wget https://github.com/mikefarah/yq/releases/download/v4.25.2/yq_linux_amd64.tar.gz -O - | tar xz && mv yq_linux_amd64 /usr/bin/yq

      - name: Test Samples
        run: ./test-samples.sh

      - name: Archive artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@v2
        with:
          name: artifacts
          path: |
            /tmp/t.png
